# Railway deployment configuration
# This file is for Railway deployment platform

# For Railway deployment, set these environment variables in the Railway dashboard:

# Database
MONGO_URI=${MONGO_URI}
MONGODB_URI=${MONGO_URI}

# Authentication
JWT_SECRET=${JWT_SECRET}

# AWS S3 (Required for persistent file storage)
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
AWS_REGION=us-east-1
AWS_S3_BUCKET=preparena-uploads

# CORS Configuration
CORS_ORIGINS=https://preparena.vercel.app,https://*.vercel.app,https://preparena.in

# Extractor Service
EXTRACTOR_URL=https://preparena-extractor.onrender.com
EXTRACTOR_AUTH_TOKEN=dev-extractor-token

# File Upload (not needed for S3, but kept for fallback)
UPLOAD_DIR=/tmp/uploads

# Environment
NODE_ENV=production